// DAT enum values matching the retail client.
namespace Acme.Dat {
    public enum SkillCategory : uint {
        Undefined = 0x00,

        Combat = 0x01,

        Other = 0x02,

        Magic = 0x03,

    };
}
